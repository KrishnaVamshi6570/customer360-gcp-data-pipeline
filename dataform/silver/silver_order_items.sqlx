config {
  type: "table",
  schema: "silver_curated",
  name: "silver_order_items"
}

SELECT
  order_id,
  product_id,
  price,
  freight_value,
  ingestion_timestamp,
  batch_id
FROM ${ref("order_items_src")}